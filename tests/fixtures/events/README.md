# `events` Fixtures

Simpan event envelope JSON atau builder data: valid, malformed payload, unsupported version, duplicate, create/update/delete, booking/payment/document/review. Jangan menyimpan secret.

File fixture ditambahkan hanya bersama test yang menggunakannya dan harus memiliki version/provenance pada metadata.

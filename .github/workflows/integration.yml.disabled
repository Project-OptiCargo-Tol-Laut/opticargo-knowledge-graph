# Compatibility marker: the implemented workflow is integration.yml.
# This file intentionally keeps the former path visible for repository history
# and must not be enabled or copied as a second workflow.
